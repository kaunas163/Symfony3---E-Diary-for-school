{% extends 'base.html.twig' %}

{% block body %}
    <h1>Kursas</h1>

    <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>Pavadinimas</th>
                <td>{{ course.title }}</td>
            </tr>
            <tr>
                <th>Aprašymas</th>
                <td>{{ course.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('courses_index') }}">Grįžti į sąrašą</a>
        </li>
        <li>
            <a href="{{ path('courses_edit', { 'id': course.id }) }}">Redaguoti</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Ištrinti">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
